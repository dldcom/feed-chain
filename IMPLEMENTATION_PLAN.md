# 먹이그물 탐험대 전체 구현 계획

문서 버전: 2.0
작성 기준일: 2026-09-07
목적: 초등 과학 수업에서 학생들이 먹이사슬과 먹이그물의 관계를 직접 움직이고 관찰하면서 기록하도록 한다.

이 문서는 현재 코드의 구조를 기준으로 앞으로 구현할 기능, 서버 권위 규칙, 화면 흐름, 재접속 정책, 테스트 기준을 한 곳에서 관리하기 위한 작업 계획이다. 이미 동작하는 기능은 유지하되, 아래의 목표 흐름과 충돌하는 부분은 단계별로 교체한다.

## 1. 확정된 제품 방향

- 학생은 생물 한 종의 역할을 맡아 2D 숲 맵을 이동한다.
- 먹이 관계의 최종 판정은 Colyseus 서버가 담당한다.
- 플레이어의 초기 개체수는 1이며, 개체수는 곧 목숨 풀이다.
- 먹기에 성공할 때마다 현재 개체수와 관계없이 정확히 1 증가한다.
- 먹히면 개체수 1이 감소한다.
  - 감소 후 개체수가 1 이상이면 3초 뒤 다른 위치에서 리스폰한다.
  - 감소 후 0이면 유령 상태로 10초 동안 이동할 수 있고, 이후 개체수 1로 리스폰한다.
- NPC의 먹기와 번식도 같은 개체수 집계에 반영한다.
- 학생이 가까이 있는 먹이를 누르면 바라보는 방향과 관계없이 먹을 수 있어야 한다. 방향이 다르다는 이유로 먹기 요청을 막거나 별도의 안내를 띄우지 않는다.
- 먹기 전용 스프라이트시트는 사용하지 않는다. 이동 스프라이트를 유지한 채 짧은 전진·진동 효과만 적용한다. 조류의 낚아채기처럼 승인된 종별 특수 연출만 별도로 사용한다.
- 맵에는 물을 넣지 않는다. `meadow-v2`와 `dirt-v2` 타일, 식물, 정사각형 수풀 모듈만 사용한다.
- 교사 화면은 일반 웹 관리 페이지가 아니라 픽셀 인디게임의 대기실처럼 보이게 만든다.

## 2. 전체 수업 흐름

```text
교사 이름 입력
  ↓
수업방 생성
  ↓
교사 대기실 ── 수업 코드 / QR / 링크 / 학생 목록 / 게임 모드 선택
  ↓
역할 자동 배정 또는 수동 수정 (학생에게 비공개)
  ↓
역할 학습 10초 (학생별 미니 Phaser 공간)
  ↓
게임 모드 플레이
  ↓
모드 결과 (개체수·관계·개인 기록·랭킹)
  ↓
다음 모드 선택 또는 전체 결과
```

게임 중 연결이 끊긴 경우의 흐름은 다음과 같다.

```text
플레이 중 연결 끊김
  ↓
PlayerState 유지 + disconnected 표시 + 상호작용 제외
  ↓ (현재 게임의 종료 시각 전까지)
같은 재접속 토큰으로 복귀
  ↓
같은 세션·종·개체수·게임 상태 복원
```

모드 결과 화면으로 넘어가는 순간 해당 모드의 재접속 예약은 만료된다. 다음 모드를 시작하면 새로운 모드 세션으로 취급한다.

## 3. 게임 모드 규칙

모든 모드의 지속 시간, 종 목록, 관계, 식물 수, NPC 수, 굶주림 규칙은 `packages/shared/src/modes.ts`의 설정으로 관리한다. 클라이언트가 임의로 모드 규칙을 바꿀 수 없도록 서버가 설정을 읽어 초기화한다.

### 3.1 모드 1 — 먹이사슬 관찰

| 구분 | 내용 |
| --- | --- |
| 시간 | 5분 |
| 종 | 매(`hawk`) → 개구리(`frog`) → 애벌레(`caterpillar`) → 토끼풀(`clover`) |
| 플레이어 역할 | 매, 개구리, 애벌레 |
| NPC | 토끼풀을 자동 생성·재생 |
| 굶주림 사망 | 사용하지 않음 |
| 학습 목표 | 먹이에서 포식자로 이어지는 한 줄의 먹이사슬 관찰 |

먹이에 성공할 때마다 플레이어 개체수가 1 증가한다. 결과 화면에는 종별 플레이어 개체수 합계, 필요 시 NPC·식물까지 포함한 생태계 총량, 개인별 개체수와 먹기 성공 횟수를 분리해서 보여준다.

### 3.2 모드 2 — 먹이사슬에서 개구리가 사라진다면?

| 구분 | 내용 |
| --- | --- |
| 시간 | 3분 |
| 플레이어 역할 | 매, 애벌레 |
| NPC | 개구리 1마리로 시작 |
| 개구리 NPC | 먹기와 번식 가능. 개체수가 늘어날 수 있으나, 고정 NPC가 0이 되었을 때의 재생성 규칙은 모드 설정으로 명시 |
| 먹이 제한 | 1분 동안 먹지 못하면 개체수 1 감소 |
| 학습 목표 | 중간 단계가 줄어들 때 애벌레와 토끼풀의 변화를 관찰 |

개구리 NPC의 초기 개체수는 1이다. 개구리가 먹이를 잘 먹으면 개체수를 늘릴 수 있지만, 학생들이 개구리를 직접 플레이하지 않으므로 개구리의 먹기·번식·피식·리스폰도 서버의 전체 개체수 기록에 포함한다.

### 3.3 모드 3 — 먹이그물 관찰

다음 14종을 모두 사용한다.

```text
동물: 매, 다람쥐, 뱀, 직박구리, 족제비, 메뚜기, 오리, 개구리, 토끼, 애벌레
생산자: 도토리, 풀, 산수유 열매(berry), 토끼풀
```

| 구분 | 내용 |
| --- | --- |
| 시간 | 5분 |
| NPC | 생산자 식물 자동 생성·재생 |
| 굶주림 사망 | 사용하지 않음 |
| 학습 목표 | 한 종이 여러 종과 연결되는 먹이그물 관찰 및 기록 |

### 3.4 모드 4 — 먹이그물에서 한 종이 사라진다면?

| 구분 | 내용 |
| --- | --- |
| 시간 | 3분 |
| 제거 종 | 교사가 선택한 한 종 |
| 처리 | 제거 종은 플레이어 역할·NPC·식물·관계에서 제외하고, 나머지 대체 관계는 유지 |
| 먹이 제한 | 1분 동안 먹지 못하면 개체수 1 감소 |
| 학습 목표 | 한 종이 사라져도 대체 관계가 있어 먹이그물 전체가 즉시 무너지지 않음을 관찰 |

제거 종이 생산자이면 해당 식물 생성도 중단한다. 동물이면 해당 종의 플레이어/NPC를 활성화하지 않는다. 제거 종의 개체수는 모드 전체 시간 동안 0으로 유지한다.

### 3.5 자동 역할 배정 밸런스 (학생 21~24명)

생산자는 식물 NPC로 생성하므로 학생 역할은 소비자 단계의 비율을 맞춘다. 자동 배정은 아래 쿼터로 역할 슬롯을 만든 뒤 학생을 무작위로 섞어 넣는다. 같은 단계에 여러 종이 있으면 해당 단계 안에서 종을 번갈아 배정해 특정 종에 학생이 몰리지 않게 한다.

| 학생 수 | 모드 1 매/개구리/애벌레 | 모드 2 매/애벌레 | 모드 3·4 최상위/2차/1차 소비자 |
| ---: | ---: | ---: | ---: |
| 21명 | 2 / 6 / 13 | 3 / 18 | 3 / 6 / 12 |
| 22명 | 2 / 6 / 14 | 3 / 19 | 3 / 7 / 12 |
| 23명 | 2 / 6 / 15 | 3 / 20 | 3 / 7 / 13 |
| 24명 | 2 / 7 / 15 | 3 / 21 | 3 / 7 / 14 |

모드 2의 개구리 1마리는 NPC로 별도 시작한다. 모드 4에서 동물 종 하나를 제거하면 해당 포식 단계의 슬롯을 남은 종에 재분배하고, 생산자를 제거하면 학생 슬롯은 그대로 둔다. 교사가 수동으로 바꾼 역할은 자동 쿼터보다 우선하며, `자동 배정하기`를 다시 누르면 같은 쿼터로 다시 섞는다.

## 4. 교사 진입과 대기실

### 4.1 교사 진입

- 처음 화면에서 교사는 학년·반이 아니라 교사 이름(닉네임)만 입력한다.
- 자유 입력을 허용하되 서버에서 앞뒤 공백, HTML 태그, 최대 길이를 정리한다.
- 학생은 기존처럼 수업 코드와 학생 닉네임으로 입장한다.
- 수업 코드는 6자리 대문자/숫자로 생성하고 방 ID와 동일하게 사용한다.

### 4.2 교사 대기실 레이아웃

기존 오른쪽 사이드바를 전체 화면 대기실로 교체한다.

왼쪽 공유 영역:

- 수업 코드 크게 표시
- QR 코드
- QR 확대 버튼과 닫기 동작
- 학생 입장용 링크와 복사 버튼
- 링크는 `VITE_PUBLIC_APP_URL`을 기준으로 `/?room=코드`를 만든다. 랜딩 화면은 `room` 쿼리를 읽어 학생 입장 폼을 미리 채운다.
- 배포 전에는 현재 접속 주소를 임시 기본값으로 사용할 수 있지만, 실제 수업에서는 환경 변수로 고정한다.

오른쪽 운영 영역:

- `학생 대기중`
- 현재 학생 수만 표시 (`5명 대기 중` 형식, 정원 대비 숫자 분수 표기 금지)
- 학생 닉네임 목록
- `게임 모드 선택`
- `자동 배정하기`
- 선택 학생의 역할 수동 지정
- 게임 시작 및 대기실 초기화

빈자리 카드나 의미 없는 이모지는 사용하지 않는다. Mona 글꼴, 제한된 픽셀 팔레트, 얇은 픽셀 프레임과 반투명 패널을 사용한다.

### 4.3 역할 배정

- 자동 배정은 버튼을 누를 때마다 서버에서 새로 섞는다.
- 자동 배정 직후 학생에게는 역할을 공개하지 않는다.
- 교사가 학생을 클릭하면 역할 선택지가 먹이사슬 단계별 행으로 나뉘어 표시된다.
  - 위: 최상위 포식자
  - 중간: 2차 소비자
  - 아래: 1차 소비자
  - 생산자는 플레이어 역할이 아닌 경우 별도 안내
- 수동 변경도 서버 명령으로만 처리한다.
- 자동 배정 결과와 개별 배정 결과는 교사 화면에서만 확인 가능하다.
- 학생 수에 따른 포식 단계별 자동 슬롯 분배는 `roleQuotaForMode`와 `roleSlotsForMode`에서 관리한다. 교사가 수동으로 바꾼 역할은 자동 배정 결과를 덮어쓴다.
- 다음 모드 자동 배정은 직전 완료 모드에서 실제로 맡았던 역할을 학생별로 기억해, 역할 정원과 단계 비율을 유지하는 범위에서 같은 학생의 종 중복을 최소화한다. 자동 배정을 다시 눌러도 이 기준은 유지되고 배정 순서만 다시 섞인다.

현재 구현은 `PlayerState.species`를 서버 전용 필드로 두고, 공개 스냅샷에는 역할을 싣지 않는다. 역할 정보는 다음 메시지 경계를 지킨다.

- `teacher_roles`: 교사 세션에만 전체 학생 역할을 전달한다.
- `role_briefing`: 각 학생 세션에 자신의 종·먹이·포식자·스킬만 전달한다.
- `public_species`: 역할 학습이 끝나고 본 게임이 시작될 때 렌더링에 필요한 종 맵을 공개한다.

클라이언트는 역할 공개 전 이전 모드의 종 맵을 비우고, 연결이 끊긴 학생은 현재 모드가 끝날 때까지 상태를 보존하되 먹기·NPC 추적 대상에서 제외한다.

## 5. 역할 학습 미니 게임

교사가 게임을 시작하면 바로 본 게임에 들어가지 않고 학생별 역할 학습 화면을 10초간 보여준다.

- 작은 Phaser 연습 공간을 사용한다.
- 실제 게임과 같은 이동 입력, 조이스틱, 스킬 버튼을 사용한다.
- 연습 공간은 서버 생태계와 분리한다. 연습 중 먹기·피식·개체수 기록은 실제 모드에 반영하지 않는다.
- 왼쪽 위에는 내 종, 내 먹이, 나를 먹는 종, 스킬 아이콘을 픽셀 아이콘으로 표시한다.
- 설명은 긴 문장 대신 아이콘과 짧은 문구를 사용한다.
- 10초 카운트다운이 끝나면 서버가 `mode_play`로 전환한다.
- 연결이 늦은 학생은 서버가 전달한 남은 시간에 맞춰 연습 화면을 시작하고, 시간이 끝난 뒤 본 게임으로 이동한다.
- 교사 화면에는 학생들이 역할을 학습 중이라는 상태만 표시한다.

Phaser 씬은 기존 `GameScene`과 공통 입력/스프라이트 코드를 재사용하되, 연습용 월드·카메라·엔티티 목록을 별도로 초기화한다. 씬 재시작 때 이전 타이머와 이벤트가 남지 않도록 `init`/`shutdown`에서 상태를 정리한다.

## 6. 본 게임 규칙과 서버 권위

### 6.1 플레이어 개체수와 생명주기

`PlayerState.populationCount`를 유일한 개체수/목숨 값으로 사용한다.

```text
초기 입장       populationCount = 1
올바른 먹기     populationCount += 1
피식            populationCount -= 1
피식 후 > 0     status = respawning, 3초 뒤 새 위치에서 active
피식 후 = 0     status = ghost, 10초 동안 이동 가능
유령 종료       populationCount = 1, 새 위치에서 active
```

- 개체수가 2나 3이어도 한 번 먹을 때마다 1 증가한다.
- 개체수 배지는 캐릭터 위에 `X3`, `X10`처럼 표시한다. `개체수`라는 글자는 표시하지 않는다.
- 유령/리스폰 중에는 먹기 대상에서 제외한다.
- 개체수 감소·증가는 서버에서만 수행한다.
- NPC도 `populationCount`, `status`, `respawnAt`, `ghostUntil`을 같은 방식으로 관리한다.

### 6.2 굶주림

- 모드 1과 3은 관찰 중심이므로 굶주림으로 목숨을 차감하지 않는다.
- 모드 2와 4는 `lastFoodAt` 기준 60초 동안 성공한 먹기가 없으면 개체수 1을 차감한다.
- 굶주림으로 1이 차감된 뒤 남은 개체수가 있으면 설정된 10초 리스폰, 0이면 10초 유령 규칙을 적용한다.
- 먹기 성공 시 허기 게이지를 회복한다.
- 연결 끊김 중에는 학생에게 불리한 굶주림 타이머가 흐르지 않도록, 연결 끊김 시작 시각을 기록하고 재접속 후 타이머 기한을 같은 시간만큼 보정한다. 전역 모드 시계는 계속 흐른다.

### 6.3 먹기 판정

- 서버는 대상 존재 여부, 활성 상태, 현재 모드의 관계, 거리, 수풀 가시성, 쿨다운을 검증한다.
- 가까운 거리의 대상은 방향과 무관하게 먹을 수 있다.
- 클라이언트는 같은 거리 판정으로 타깃 원만 표시한다. 방향이 다르다는 이유로 요청 자체를 보내지 않거나 별도 오류를 표시하지 않는다.
- 서버의 거리 판정과 클라이언트 예측 판정은 shared 패키지의 동일한 함수로 유지한다.
- 연결이 끊긴 플레이어는 먹기 대상과 NPC 추적 대상에서 제외한다.
- 식물은 수풀 안에서도 먹이 안내를 위해 보이게 유지할지 여부를 모드 설정으로 고정한다. 플레이어/NPC는 서로 다른 수풀 구역에 있으면 가려진다.

### 6.4 NPC와 번식

- 모드 설정이 허용한 NPC만 생성한다.
- NPC가 올바른 먹이를 먹으면 NPC 자신의 개체수와 종별 총량이 증가한다.
- 번식 조건, 재사용 대기 시간, 종별 최대 개체수는 `GameModeConfig`에 둔다.
- 모드 2의 개구리 NPC는 기본 1마리이며 먹기와 번식이 가능하다.
- 제거 모드에서 고정된 제거 종은 번식·리스폰으로 다시 살아나지 않는다.
- NPC의 먹기·번식·피식·굶주림·리스폰은 개인 기록과 모드 타임라인에 남긴다.

## 7. 재접속 정책

### 7.1 목표

학생이 태블릿에서 잠깐 튕겨도 현재 게임이 끝나기 전까지 같은 종과 개체수로 돌아오게 한다. Colyseus 메모리 상태만 사용하므로 서버 프로세스가 살아 있는 동안의 복구 정책이다.

### 7.2 서버 구현

현재 `onDrop`의 30초 고정값과 `updateTimedStatuses`의 30초 삭제 조건을 제거하고, 모드별 종료 시각을 기준으로 바꾼다.

필요한 상태:

- `modeInstanceId`: 모드마다 증가하는 게임 세션 ID
- `modeStartedAt`
- `modeEndsAt`
- `reconnectDeadline`
- 플레이어별 `disconnectedAt` 및 일시정지한 개인 타이머 누적값

권장 흐름:

1. `mode_play` 시작 시 서버가 `modeStartedAt`과 `modeEndsAt`을 설정한다.
2. 예기치 않은 연결 끊김이면 `connected=false`로 바꾸고 `allowReconnection(client, remainingSeconds)`를 호출한다.
3. `remainingSeconds`는 `modeEndsAt - Date.now()`로 계산하며, 최소 0초로 제한한다.
4. 연결이 끊긴 플레이어의 `PlayerState`는 삭제하지 않는다.
5. 연결 끊김 중에는 먹기 대상, NPC 추적 대상, 공격 대상에서 제외한다.
6. 개인 허기·굶주림·스킬·리스폰 타이머는 연결 끊김 시간을 보정해 재접속 후 이전 진행 상태가 유지되게 한다.
7. `onReconnect`에서 같은 `sessionId`의 상태를 `connected=true`로 복원하고, 토큰·모드 세션 ID가 현재 모드와 일치하는지 검증한다.
8. `mode_result`로 전환하는 순간 남은 재접속 예약을 만료시키고 연결이 끊긴 플레이어를 정리한다.
9. 의도적인 `leave(true)`, 세션 토큰 삭제, 다른 기기 입장은 기존 플레이어를 복원하지 않고 새 세션으로 처리한다.

### 7.3 클라이언트 구현

- `room.reconnectionToken`과 역할을 현재처럼 `sessionStorage`에 저장한다.
- 새로고침 시 토큰이 있으면 `client.reconnect(token)`을 시도한다.
- `/game-test`는 독립 테스트 화면이므로 실제 수업 재접속을 자동으로 시도하지 않는다.
- 재접속 중에는 게임 캔버스 위에 작은 연결 복구 표시를 띄운다.
- 복구 성공 시 서버 스냅샷으로 위치·개체수·상태·허기·쿨다운을 다시 동기화한다.
- 게임 종료 후 토큰이 만료되면 새로고침 화면에서 “게임이 끝났습니다. 선생님에게 새 입장을 요청하세요.”를 표시한다.
- 새 모드가 시작되면 이전 모드의 토큰으로 역할을 되살리지 않는다.

### 7.4 재접속의 한계

- Colyseus의 방 상태는 메모리 기반이므로 서버 재시작, 프로세스 장애, 방 강제 종료 후에는 복원할 수 없다.
- 학생이 다른 기기나 다른 브라우저에서 들어가면 `sessionStorage` 토큰이 없으므로 새 플레이어로 입장한다.
- 수업이 끝난 뒤 결과 조회까지 영구 재접속을 허용하지 않는다. 필요한 경우 결과 JSON을 별도 저장하는 기능을 추가한다.

## 8. 방 수명과 수업 코드 정리

수업 코드가 방치되지 않도록 방 생성 시 `roomExpiresAt = createdAt + 24시간`을 기록한다.

- 24시간이 지나면 새 입장을 막고, 교사·학생 연결을 종료한 뒤 방을 dispose한다.
- 방 종료 시 재접속 토큰과 메모리 맵(`disconnectedAt`, 먹기 기록, NPC 추적, 결과 캐시)을 모두 정리한다.
- 현재는 메모리 방이므로 별도 DB 레코드가 없다. 향후 방 정보를 저장하더라도 `expiresAt` 인덱스/정리 작업으로 동일한 TTL을 적용한다.
- 빈 방은 Colyseus의 기본 자동 dispose를 활용할 수 있지만, 교사가 대기실을 열어 둔 동안에는 24시간 TTL을 넘기지 않는다.
- 수업 코드 재사용은 허용하지 않는다. 만료 또는 종료된 코드는 새 방 생성 시 다시 발급하지 않는다.

## 9. 학생 화면과 게임 HUD

### 9.1 공통 UI 규칙

- 전역 글꼴은 `@import url('https://cdn.jsdelivr.net/gh/MonadABXY/mona-font/web/mona.css');`를 사용하고 weight는 기본 400으로 통일한다.
- 웹 대시보드처럼 보이는 큰 카드, 강한 그림자, 과도한 그라데이션, 의미 없는 이모지를 사용하지 않는다.
- 픽셀 프레임, 제한된 팔레트, 반투명 패널을 사용해 월드가 가려지지 않게 한다.
- 모바일/태블릿 가로 화면을 우선하고 `touch-action: none`, 멀티터치 포인터 추적을 유지한다.

### 9.2 플레이 HUD

왼쪽 위:

- 내 캐릭터 픽셀 아이콘
- 닉네임만 작게 표시
- 닉네임 아래 고기 아이콘 기반 허기 게이지
- 적과 먹이 아이콘을 두 줄로 배치하고 아이콘 사이에는 한 칸 간격을 둔다.
- `내 적`, `내 먹이`, `배고픔`, `개체수` 같은 설명 글자는 최소화하거나 제거한다.

월드 캐릭터:

- 캐릭터 밑의 큰 원과 캐릭터 아래 이름을 제거한다.
- 닉네임은 캐릭터 위에 매우 작게 표시한다.
- 개체수는 캐릭터 위에 `X1`, `X3`, `X10` 배지로 표시한다.
- 먹을 수 있는 대상에 접근했을 때 나타나는 타깃 원만 유지한다.

오른쪽 아래:

- A(먹기), B(스킬) 두 버튼을 세로로 배치한다.
- 글자만 크게 표시하고 버튼 안의 이모지는 사용하지 않는다.
- 두 버튼 모두 반투명 픽셀 프레임, 눌림 시 밝기 변화만 사용한다.
- 조이스틱도 색·그림자·불투명 배경을 최소화한다.
- 왼손 조이스틱과 오른손 A/B 버튼이 동시에 동작하도록 Phaser 추가 포인터와 Pointer ID를 분리한다.

## 10. 월드와 수풀

- 배경은 `meadow-v2`를 전체 타일링하고, 비대칭 곡선의 `dirt-v2` 산책로를 겹친다.
- 물과 물 충돌은 추가하지 않는다.
- 수풀은 정사각형 모듈 하나를 여러 개 겹쳐 하나의 `coverId` 구역으로 만든다.
- 같은 `coverId` 안의 캐릭터끼리는 서로 보인다.
- 다른 구역 또는 바깥에 있는 관찰자에게는 수풀 안의 플레이어/NPC가 보이지 않는다.
- 관찰자가 현재 들어간 수풀만 반투명으로 낮춘다.
- 수풀의 시각 모듈 크기와 충돌/가시성 판정 영역은 shared 월드 상수로 함께 관리한다.
- 타일·수풀 PNG는 현재 384px/64색 압축 기준을 유지하고, 실제 태블릿에서 메모리와 로딩 시간을 측정한 뒤 추가 압축 여부를 결정한다.

## 11. 결과 화면과 기록

모드가 끝나면 서버가 `ModeResult`를 확정하고, 클라이언트는 결과 화면으로 이동한다.

모드 결과에 포함할 내용:

- 모드 번호·제목·진행 시간
- 종별 플레이어 개체수 합계와 NPC·식물 포함 생태계 총량의 구분
- 종별 최종 개체수와 최고 개체수
- 학생별 종, 최종 개체수, 먹기 성공 횟수, 먹힌 횟수, 생존 시간
- 종별 또는 학생별 랭킹
- 실제로 관찰된 먹이 관계와 관계별 발생 횟수
- 5초 간격 개체수 타임라인
- 모드 4에서 사라진 종과 대체 관계

기존 먹이그물 화면의 빨간색(실제 관찰)·파란색(학생이 보완한 관계) 표현은 유지한다. 교사는 전체 JSON과 PNG를 저장할 수 있고, 학생은 본인에게 필요한 요약만 본다.

## 12. 구현 대상 파일과 책임

### shared 패키지

- `packages/shared/src/modes.ts`: 네 모드의 종, 역할 슬롯, 시간, NPC, 식물, 굶주림, 재접속 관련 설정
- `packages/shared/src/phases.ts`: `lobby → mode_setup → role_reveal → mode_play → mode_result` 흐름과 레거시 단계 호환
- `packages/shared/src/messages.ts`: 역할 공개, 연습 종료, 재접속 상태, 모드 결과 메시지 타입
- `packages/shared/src/food-web.ts`: 관계 및 거리/가시성 규칙
- `packages/shared/src/species.ts`: 종·스킬·표시 이름·먹이/적 메타데이터
- `packages/shared/src/world.ts`: 맵 경계, 수풀, 스폰, 장애물

### server

- `apps/server/src/rooms/schema.ts`: 공개 상태와 서버 전용 역할/재접속 상태 분리
- `apps/server/src/rooms/EcosystemRoom.ts`: 모드 준비·역할 배정·역할 공개·게임 종료·재접속·개체수·NPC·TTL
- `apps/server/src/main.ts`: 방 생성 옵션, 서버 종료/헬스 체크, 필요 시 TTL 정리 훅

### client

- `apps/client/src/network/gameClient.ts`: 방 생성·입장·재접속·개인 역할 메시지·모드 세션 처리
- `apps/client/src/store/gameStore.ts`: 연결 상태, 역할 공개 정보, 연습 타이머, 모드 결과 저장
- `apps/client/src/types.ts`: 공개 스냅샷과 개인 역할 정보 타입
- `apps/client/src/App.tsx`: 교사 로비, 역할 학습, 게임, 결과 화면 라우팅
- `apps/client/src/screens/TeacherLobbyScreen.tsx`(신규): QR/링크/학생 목록/모드/배정 운영 화면
- `apps/client/src/screens/RoleRevealScreen.tsx`: 10초 Phaser 연습 공간과 개인 관계 HUD
- `apps/client/src/screens/GameScreen.tsx`: 실제 플레이 화면
- `apps/client/src/components/GameHud.tsx`, `VirtualJoystick.tsx`: 반투명 픽셀 HUD와 멀티터치
- `apps/client/src/game/*`: 실제 게임과 연습 게임의 공통 스프라이트·입력·월드 코드
- `apps/client/src/styles/global.css`: Mona 글꼴과 픽셀 인디게임 레이아웃

### 테스트와 문서

- `tests/rules/rules.test.ts`: 모드 설정, 거리 먹기, 개체수, 수풀, 재접속 만료 계산
- `tests/e2e/class-flow.spec.ts`: 교사 생성 → 학생 입장 → 배정 → 10초 학습 → 모드 플레이 → 결과
- `scripts/smoke-multiplayer.mjs`: 21~24명 이동·먹기·피식·유령·스킬·재접속
- `scripts/smoke-modes.mjs`: 네 모드의 시작·종료·제거 종·NPC·결과
- `tests/multiplayer/reconnect.spec`(신규): 모드 종료 전/후 재접속과 토큰 실패
- `README.md`: 실행 명령, 링크 환경 변수, 현재 지원 흐름

## 13. 구현 순서

### 단계 0 — 계약 고정

- [ ] 현재 코드와 목표 문서의 차이 목록화
- [ ] `GamePhase`, `GameModeConfig`, `ModeResult`, 역할 공개 메시지 확정
- [ ] 공개 스냅샷과 개인 역할 정보 분리 방식 결정
- [ ] 모드별 역할 슬롯과 기본 밸런스 값을 표로 고정

완료 기준: 서버·클라이언트가 같은 타입을 사용하고, 역할이 공개되는 시점이 문서로 명확하다.

### 단계 1 — 방과 교사 대기실

- [ ] 교사 닉네임 진입 화면 적용
- [ ] 전체 화면 교사 로비 제작
- [ ] 수업 코드/QR/링크/복사/확대 기능 구현
- [ ] 학생 수를 현재 인원만 표시
- [ ] 24시간 방 TTL과 만료 처리 추가
- [ ] 모드 선택 문구를 `게임 모드 선택`으로 통일

완료 기준: 교사가 방을 만들고 학생이 들어오면 두 화면이 서로 다른 역할에 맞는 대기실을 보여준다.

### 단계 2 — 역할 배정과 역할 학습

- [ ] 먹이사슬 단계별 역할 선택 UI
- [ ] 서버 자동 랜덤 배정 및 반복 랜덤 배정
- [ ] 교사 전용 수동 배정
- [ ] 학생 공개 스냅샷에서 역할 정보 제거
- [ ] 개인 역할·먹이·적·스킬 메시지 구현
- [ ] 10초 미니 Phaser 연습 공간과 서버 기준 카운트다운 구현
- [ ] 학습 종료 후 모드 플레이 전환

완료 기준: 학생은 게임 시작 전 다른 학생의 역할과 교사의 배정 과정을 알 수 없고, 본인 역할을 10초 동안 직접 조작해 볼 수 있다.

### 단계 3 — 네 모드 전환

- [ ] `startMode`를 `mode_setup → role_reveal → mode_play` 흐름으로 분리
- [ ] 모드 1 식물 생성과 먹이사슬 기록
- [ ] 모드 2 개구리 NPC 1마리, 먹기·번식·굶주림
- [ ] 모드 3 14종 전체 생성 및 관계 기록
- [ ] 모드 4 교사 제거 종 선택, 대체 관계와 생산자 제거
- [ ] 모드별 시간·정지·강제 종료·결과 전환

완료 기준: 교사가 네 모드를 임의 순서로 시작하고, 각 모드 결과가 이전 모드의 상태와 섞이지 않는다.

### 단계 4 — 개체수와 밸런스

- [ ] 플레이어/NPC 개체수 증가·감소 공통 함수 정리
- [ ] 3초 리스폰·10초 유령·안전 스폰 검증
- [ ] 1분 굶주림 규칙과 연결 끊김 중 타이머 보정
- [ ] 식물 재생·NPC 번식 상한
- [ ] 결정적 시드 기반 모드 시뮬레이션
- [ ] 5분/3분 동안 특정 종이 너무 빨리 0이 되거나 무한 증가하지 않는지 조정

완료 기준: 정상적인 21~24명 플레이에서 모드 1·3은 관계 관찰이 충분히 발생하고, 모드 2·4는 변화가 보이면서도 초반에 게임이 끝나지 않는다.

### 단계 5 — 게임 종료 시점 재접속

- [ ] 30초 하드코딩 제거
- [ ] `modeEndsAt` 기반 `allowReconnection` 구현
- [ ] 연결 끊긴 플레이어의 상호작용 제외
- [ ] 개인 타이머 보정 및 상태 보존
- [ ] 모드 결과 전환 시 토큰/예약 만료
- [ ] 새 모드에서 이전 세션 재사용 방지

완료 기준: 플레이 중 어느 시점에 튕겨도 같은 모드가 끝나기 전에는 같은 세션·종·개체수로 복귀하고, 결과 화면 이후에는 복귀가 거절된다.

### 단계 6 — 결과와 수업 기록

- [ ] 종별 개체수 합계와 개인 랭킹
- [ ] 모드별 타임라인
- [ ] 먹이사슬/먹이그물 그래프
- [ ] 교사 전용 운영 지표
- [ ] 전체 JSON/PNG 다운로드
- [ ] 다음 모드 시작 시 이전 결과 보존

### 단계 7 — 태블릿 QA와 배포

- [ ] 10~11인치 가로 태블릿 입력 테스트
- [ ] 조이스틱을 누른 채 A/B 버튼을 누르는 멀티터치 테스트
- [ ] 새로고침·잠금 해제·Wi-Fi 전환 재접속 테스트
- [ ] 21~24명 동시 접속과 24시간 TTL 테스트
- [ ] 모바일 이미지 로딩·메모리·프레임 측정
- [ ] 배포 URL로 QR 접속 테스트
- [ ] 서버/클라이언트 빌드 및 콘솔 오류 0건 확인

## 14. 테스트 시나리오

### 규칙 단위 테스트

- 모든 모드의 종·시간·제거 종 설정
- 먹이 관계와 가까운 거리의 방향 무관 먹기
- 잘못된 관계 거절 및 쿨다운
- 개체수 1→2→1→0→유령→1 생명주기
- NPC 먹기·번식·고정 제거 종
- 60초 굶주림과 10초 리스폰
- 같은 수풀 구역의 가시성, 다른 수풀 구역의 은폐
- `modeEndsAt` 기준 재접속 허용 시간 계산

### 멀티플레이 테스트

- 21~24명 입장과 포식 단계별 역할 슬롯 배분
- 자동 배정 버튼을 두 번 눌렀을 때 결과가 다시 섞임
- 교사만 수동 역할 변경 가능
- 역할 공개 전 학생 스냅샷에 다른 학생 역할이 없음
- 10초 역할 학습 후 모든 학생이 같은 모드에 입장
- 플레이 중 학생 한 명을 강제 연결 종료
- 모드 종료 1초 전 재접속 성공
- 모드 결과 진입 후 같은 토큰 재접속 실패
- 재접속 학생의 세션 ID·종·개체수·위치·기록 보존
- 연결 끊긴 학생이 먹히거나 NPC의 추적 대상이 되지 않음

### E2E 수업 흐름

```text
교사 닉네임 입력
→ 방 생성
→ 학생 3명 이상 입장
→ 역할 자동 배정
→ 학생 역할 학습 화면
→ 모드 1 플레이
→ 학생 연결 종료/복귀
→ 모드 1 결과
→ 모드 2·3·4 중 하나 시작
→ 제거 종 결과와 전체 기록 확인
```

## 15. 완료 조건

다음 조건을 모두 만족하면 이번 구현 범위를 완료로 본다.

- 교사와 학생의 진입·대기실·역할 공개·플레이·결과 흐름이 끊기지 않는다.
- 학생은 역할 공개 전 교사의 배정 결과나 다른 학생의 역할을 알 수 없다.
- 네 가지 모드가 서로의 개체수·관계·타임라인을 오염시키지 않는다.
- 개체수 증가, 피식, 3초 리스폰, 10초 유령, NPC 먹기·번식이 서버 기록과 화면 배지에 일치한다.
- 플레이 중 연결이 끊긴 학생은 해당 게임 종료 전까지 같은 세션과 종으로 복귀한다.
- 모드가 끝난 뒤에는 이전 토큰으로 게임에 다시 끼어들 수 없다.
- 24명 동시 접속, 태블릿 멀티터치, QR 입장, JSON 결과 저장을 검증한다.
- 수업 코드와 방은 생성 후 24시간을 넘겨 메모리에 남지 않는다.
- 디자인은 Mona 400, 반투명 픽셀 HUD, 작은 닉네임·개체수 배지, A/B 세로 버튼, 수풀 은폐 규칙을 일관되게 따른다.

## 16. 현재 코드 기준 주요 변경 지점

현재 구현에는 네 모드 설정, 서버 권위 이동/먹기, 개체수·유령·NPC, 모드 결과, 역할 비공개, 10초 역할 학습, 전체 교사 대기실, 모드 종료 시각 기준 재접속이 연결되어 있다.

다음은 구현 이후 운영·고도화 순서다.

1. Playwright 브라우저가 설치된 환경에서 교사→학생→역할 학습→본 게임 E2E를 실행한다.
2. 실제 배포 URL을 `VITE_PUBLIC_APP_URL`에 넣고 QR 입장을 확인한다.
3. 태블릿에서 멀티터치, Wi-Fi 전환, 화면 회전·안전 영역을 점검한다.
4. 실제 수업 데이터를 기준으로 모드별 NPC·식물 생성량과 굶주림 속도를 조정한다.
5. 새 네 모드가 안정화되면 기존 레거시 `round_1`, `round_2`, `experiment_a/b` 흐름의 제거 여부를 결정한다.

이 문서의 규칙과 완료 조건이 바뀌면 먼저 이 파일과 `packages/shared`의 타입·설정을 함께 수정한 뒤 서버와 클라이언트를 변경한다.

## 17. 구현 현황 (2026-09-07)

이번 단계에서 계획의 핵심 흐름을 실제 코드에 연결했다.

- 교사 닉네임 진입, 전체 화면 교사 대기실, 현재 학생 수, 수업 코드·QR·링크·복사·QR 확대 UI를 적용했다.
- `VITE_PUBLIC_APP_URL`이 있으면 그 주소를 QR/링크에 사용하고, 없으면 현재 브라우저 주소를 임시 기본값으로 사용한다.
- 수업방은 생성 시점부터 24시간 TTL을 가지며, 만료 시 방을 잠그고 연결을 종료한다.
- 방 정원은 교사 1명 + 학생 최대 24명이며, 21~24명 자동 배정은 포식 단계별 쿼터를 사용한다.
- 자동 배정은 선택한 모드의 포식 단계별 쿼터를 만든 뒤 같은 단계 안에서 종을 섞고, 직전 완료 모드와 같은 학생의 종 중복을 가능한 만큼 줄인다. 다시 누르면 같은 쿼터와 중복 최소화 기준으로 배정 순서만 다시 섞는다. 수동 지정 뒤에는 같은 모드로 시작할 때 수동 결과를 보존하며, 인원이 바뀌면 다음 시작 때 쿼터를 다시 계산한다.
- 역할은 공개 Colyseus 스냅샷에서 제거하고 `teacher_roles`(교사 전용), `role_briefing`(본인 전용), `public_species`(게임 시작 후 공개) 메시지로 분리했다.
- 게임 시작 뒤 학생별 10초 Phaser 역할 학습 화면을 거친 다음 서버가 `mode_play`로 전환한다.
- 네 모드의 시작·종료·제거 종·개구리 NPC·식물·개체수·결과 타임라인을 기존 서버 권위 로직과 연결했다.
- 모드 결과에 플레이어·NPC·식물 개체수 합계를 분리해 저장하고, 결과 화면에서 학생 합계와 생태계 전체 합계를 함께 표시한다.
- 플레이 중 재접속 허용 시간을 현재 모드의 종료 시각까지로 계산하고, 끊긴 플레이어는 먹기·NPC 추적에서 제외한다. 모드 결과 이후에는 재접속을 거절한다.
- `chainOrder` 결과 그래프는 수업 설명 순서인 매 → 개구리 → 애벌레 → 토끼풀 방향으로 표시한다.
- 역할·관계 UI의 도토리·토끼풀 아이콘은 식물 에셋을 직접 사용하고, 본 게임과 역할 학습 화면의 연결 끊긴 플레이어는 먹기 대상에서 제외한다.

검증 결과:

- `npm run build` 통과
- `npm run typecheck` 통과
- `npm test` 통과 (12개 규칙 테스트)
- `scripts/smoke-modes.mjs` 통과 (네 모드 및 개체수 생명주기)
- `scripts/smoke-multiplayer.mjs` 통과 (21~24명, 비공개 역할, 이동·먹기·유령·스킬·재접속)

배포 전에 남은 확인:

- Playwright 브라우저 바이너리를 설치한 환경에서 `npm run test:e2e` 실행
- 실제 배포 도메인을 `VITE_PUBLIC_APP_URL`에 넣은 뒤 QR 접속 확인
- 태블릿 기기에서 조이스틱과 A/B 버튼 멀티터치, Wi-Fi 전환 재접속 확인
- 장시간(24시간) TTL과 모드 종료 직전 재접속을 운영 환경에서 확인
