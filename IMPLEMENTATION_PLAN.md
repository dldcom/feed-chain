# 먹이그물 생태계 게임 구현 계획

## 1. 프로젝트 목표

초등학교 과학 수업의 "생물의 먹고 먹히는 관계"를 학생들이 직접 경험하고, 플레이 기록으로 먹이사슬과 먹이그물을 구성하는 23인용 실시간 웹게임을 만든다.

게임의 핵심 흐름은 다음과 같다.

1. 학생들이 서로 다른 생물 역할로 2D 맵을 돌아다닌다.
2. 자신이 먹을 수 있는 생물을 판단하고 먹는다.
3. 실제 먹기 기록을 빨간 선으로 시각화한다.
4. 학생들이 발생하지 않은 먹이 관계를 파란 선으로 보완한다.
5. 특정 생물이 사라진 생태계의 개체 수 변화를 비교한다.

점수 경쟁이나 최후 생존자가 중심이 되지 않도록 하고, 관계 발견과 반 전체 먹이그물 완성을 핵심 목표로 삼는다.

## 2. 제품 및 디자인 원칙

### 2.1 ZEP·Roblox 스타일의 게임 화면

UI는 일반적인 웹 서비스나 학습지처럼 보이면 안 된다. 게임을 실행한 뒤에는 학생이 웹페이지가 아니라 하나의 게임 공간에 들어왔다고 느껴야 한다.

- 2D 탑다운 타일맵과 캐릭터 스프라이트를 중심으로 구성한다.
- 전체 화면 게임 캔버스 위에 HUD를 겹쳐 표시한다.
- 카드 목록, 긴 세로 스크롤, 웹 대시보드형 레이아웃을 학생 화면에서 사용하지 않는다.
- 큰 아이콘, 짧은 문장, 둥근 게임 패널, 명확한 상태 색상을 사용한다.
- 화면 전환은 페이지 이동보다 게임 장면 전환처럼 연출한다.
- 역할 공개, 라운드 시작, 맵 축소, 먹힘, 리스폰에 짧은 애니메이션과 효과를 사용한다.
- 태블릿 가로 화면을 기준으로 하고, 세로 화면에서는 회전 안내를 표시한다.

### 2.2 기능 우선, 아트 후보정

초기 구현에서도 레이아웃과 조작 구조는 게임답게 만들지만, 고급 아트와 세부 스타일링은 핵심 기능을 완성한 뒤 진행한다.

1. 기본 타일·임시 스프라이트로 전체 게임 루프 구현
2. 멀티플레이와 수업 진행 안정화
3. HUD, 메뉴, 결과 화면의 정보 구조 확정
4. 실제 태블릿 플레이테스트
5. Figma에서 최종 비주얼 후보정
6. 확정된 디자인을 게임에 반영

Figma 작업 전에도 조이스틱과 액션 버튼 위치, HUD 정보 우선순위, 화면 안전 영역은 최종 구조에 가깝게 유지한다. 기능 구현 중 UI 구조가 계속 바뀌어 Figma 작업을 반복하는 일을 줄이기 위함이다.

## 3. 기술 스택

| 영역 | 기술 | 역할 |
| --- | --- | --- |
| 언어 | TypeScript | 클라이언트·서버·게임 규칙 타입 공유 |
| 저장소 | npm workspaces | 클라이언트·서버·공통 패키지 통합 관리 |
| 웹 셸 및 HUD | React + Vite | 입장, 역할 공개, HUD, 교사 화면, 결과 화면 |
| 2D 게임 | Phaser | 타일맵, 캐릭터, 카메라, 충돌, 애니메이션, 터치 입력 |
| 맵 제작 | Tiled Map Editor | ZEP 스타일 타일맵과 충돌·리스폰 영역 제작 |
| 실시간 서버 | Node.js + Colyseus | 방, 위치, 라운드, 점수, 먹기 판정 동기화 |
| 먹이그물 | D3.js + SVG | 개체·생물 노드와 빨간색·파란색 방향선 표현 |
| 단위 테스트 | Vitest | 먹이 관계, 점수, 스킬, 굶주림 규칙 검증 |
| 통합 테스트 | Playwright | 입장부터 결과 화면까지 브라우저 흐름 검증 |
| 저장 | 1차 메모리·JSON, 추후 PostgreSQL | 수업 결과 저장 및 내보내기 |

Three.js는 사용하지 않는다. 이 게임은 3D 공간보다 가독성이 높은 2D 타일맵, 간단한 충돌, 많은 동시 접속자의 안정성이 중요하다.

## 4. 프로젝트 구조

```text
feed-chain/
├─ apps/
│  ├─ client/
│  │  ├─ src/
│  │  │  ├─ game/
│  │  │  │  ├─ scenes/
│  │  │  │  ├─ entities/
│  │  │  │  ├─ controls/
│  │  │  │  └─ effects/
│  │  │  ├─ screens/
│  │  │  ├─ hud/
│  │  │  ├─ network/
│  │  │  └─ styles/
│  │  └─ public/assets/
│  │     ├─ maps/
│  │     ├─ tiles/
│  │     ├─ animals/
│  │     ├─ effects/
│  │     └─ ui/
│  └─ server/
│     └─ src/
│        ├─ rooms/
│        ├─ systems/
│        ├─ simulation/
│        └─ persistence/
├─ packages/
│  └─ shared/
│     └─ src/
│        ├─ species.ts
│        ├─ food-web.ts
│        ├─ skills.ts
│        ├─ scoring.ts
│        ├─ phases.ts
│        └─ messages.ts
├─ tests/
│  ├─ rules/
│  ├─ multiplayer/
│  └─ e2e/
└─ package.json
```

## 5. 수업 및 게임 진행 상태

```text
대기실
→ 역할 공개
→ 1판
→ 실제 먹이그물 확인
→ 파란 선 추가
→ 2판
→ 최종 먹이그물 확인
→ 생태계 실험 A
→ 생태계 실험 B
→ 결과 비교 및 정리
```

서버가 현재 단계를 관리하며 교사만 다음 단계로 전환할 수 있다. 학생 화면은 현재 단계에 맞춰 자동으로 변경된다.

### 교사 기능

- 6자리 수업 코드 생성
- 접속 학생과 연결 상태 확인
- 역할 자동·수동 배정
- 라운드 시작, 일시정지, 재개, 종료
- 남은 시간 조절
- 특정 생물 제거 선택
- 먹이그물 선 검토 및 확정
- 실험 A·B 실행
- 결과 JSON 및 이미지 저장

### 학생 기능

- 닉네임과 수업 코드로 입장
- 자신의 생물과 먹이 관계 확인
- 가상 조이스틱으로 이동
- 먹기 및 생물 스킬 사용
- 배고픔, 쿨타임, 관계 발견도 확인
- 유령 상태로 관찰 후 같은 생물로 리스폰
- 먹이그물에 파란 선 추가

학생 계정은 만들지 않는다. 수업용 닉네임만 사용하고 장기 개인정보 저장은 하지 않는다.

## 6. 1·2판 게임 규칙

### 6.1 먹기 판정

학생이 먹기 버튼을 누르면 서버가 다음 순서로 판정한다.

1. 먹기 범위 안에 대상이 있는지 확인한다.
2. 공격자가 유령, 배탈, 쿨타임 상태인지 확인한다.
3. 공격자와 대상 사이의 먹이 관계를 확인한다.
4. 대상이 방어 스킬을 사용하는지 확인한다.
5. 거리와 상태가 유효하면 먹기 성공 이벤트를 기록한다.

먹기 결과는 클라이언트가 아니라 서버가 결정한다.

### 6.2 관계 발견 점수

- 올바른 먹이를 처음 먹음: 기본 1점 + 최초 발견 보너스 1점
- 이미 먹어본 종류를 다시 먹음: 0.1점
- 먹힘: 감점 없음
- 먹을 수 없는 생물 공격: 점수 감점 없음
- 최종 순위, 우승자, 최후 생존자: 표시하지 않음

학생 HUD에는 숫자 경쟁보다 다음 정보를 우선 표시한다.

```text
관계 발견 점수 4.2
내 먹이 관계 2/3
우리 반 먹이그물 11/18
```

### 6.3 잘못된 공격과 배탈

먹을 수 없는 생물이 실제 공격 범위 안에 있을 때 먹기 버튼을 누르면 다음 페널티를 적용한다.

- 이동 2초 정지
- 먹기와 스킬 2초 비활성화
- 비틀거리는 애니메이션과 소용돌이 이펙트
- `잘못 먹어서 배탈이 났어요!` 안내

공격 범위 안에 아무 대상도 없을 때는 배탈로 처리하지 않는다.

### 6.4 먹힘, 유령, 리스폰

- 먹히면 10초 동안 유령이 된다.
- 유령은 자유롭게 이동하고 관찰할 수 있다.
- 유령은 먹거나 먹힐 수 없다.
- 10초 뒤 원래 생물 역할로 리스폰한다.
- 서버가 포식자와 멀리 떨어진 안전한 리스폰 지점을 선택한다.
- 연결이 끊기면 30초 안에 같은 역할로 재접속할 수 있다.

### 6.5 굶주림

- 배고픔은 시간에 따라 감소한다.
- 먹이를 먹으면 배고픔이 회복된다.
- 1·2판에서는 배고픔이 0이 되어도 죽지 않는다.
- 배고픔이 0이면 이동속도가 30% 감소한다.

## 7. 생물 스킬

모든 생물은 하나의 액티브 스킬만 가진다. 스킬 버튼은 역할과 관계없이 항상 같은 위치에 둔다.

| 생물 | 스킬 | 기본 효과 |
| --- | --- | --- |
| 메뚜기 | 높이뛰기 | 바라보는 방향으로 빠르게 도약 |
| 애벌레 | 몸 말기 | 3초간 이동 불가, 먹히지 않음, 쿨타임 20초 |
| 토끼 | 전력 질주 | 3초간 이동속도 35% 증가 |
| 다람쥐 | 재빠른 도약 | 짧은 거리를 순간적으로 이동 |
| 개구리 | 폴짝 점프 | 장애물을 넘는 짧은 점프 |
| 직박구리 | 날아오르기 | 짧은 거리의 장애물 통과 |
| 오리 | 빠른 발놀림 | 4초간 이동과 방향 전환 향상 |
| 뱀 | 잠복 | 제자리에서 3초간 반투명 |
| 족제비 | 재빠른 추격 | 2.5초간 이동속도 30% 증가 |
| 매 | 급강하 | 3초간 이동속도 30% 증가, 쿨타임 20초 |

스킬은 추격, 도주, 이동, 관찰만 보조한다. 먹을 수 없는 생물을 먹게 하거나 다른 종을 직접 공격하는 기능은 만들지 않는다.

애벌레의 몸 말기는 돌처럼 둥글고 단단하게 보이도록 표현하되, 이름과 설명은 실제 애벌레가 돌로 변한다는 오해가 생기지 않도록 한다. 방어 중인 애벌레를 공격한 포식자에게는 배탈 페널티를 주지 않고 `애벌레가 몸을 단단히 말았어요!`라고 안내한다.

## 8. 맵과 캐릭터

### 8.1 맵 레이어

초기 버전은 `4,800 × 3,000px` 크기의 하나의 숲 맵으로 시작한다. 중앙 교차로와 네 개의 탐색 구역을 두고, 23명이 흩어져도 먹이와 다른 생물을 꾸준히 만날 수 있도록 리스폰 후보 30개 이상과 식물 생성 지점 45개 이상을 배치한다.

```text
Ground       바닥
Decoration   나무, 바위, 꽃
Collision    통과할 수 없는 영역
OuterWall    3분 후 닫히는 외곽 벽
InnerWall    4분 후 닫히는 내부 벽
Spawn        생물별 리스폰 후보 지점
Plants       식물 NPC 생성 위치
```

물가와 풀숲은 초기 게임 규칙에 포함하지 않는다. 필요하면 장식 타일로만 사용하고 충돌이나 생물 스킬과 연결하지 않는다.

### 8.2 맵 축소

- 0:00~3:00: `4,800 × 3,000px` 전체 맵 이용
- 3:00: `3,600 × 2,300px` 영역으로 외곽 통로 폐쇄
- 4:00: `2,400 × 1,500px` 중앙 영역으로 추가 폐쇄
- 5:00: 게임 종료

자기장 피해는 없다. 벽이 닫히기 전에 외곽에 남은 학생은 가장 가까운 내부 안전 지점으로 이동시킨다.

### 8.3 초기 아트

- 타일은 단순하지만 색상 대비와 통행 가능 영역이 명확해야 한다.
- 캐릭터는 생물별 실루엣과 대표색을 다르게 한다.
- 걷기, 먹기, 배탈, 유령, 스킬 상태가 즉시 구분되어야 한다.
- 임시 에셋도 단순 도형만 띄우는 개발자 화면처럼 보이지 않도록 최소한의 스프라이트와 게임 HUD를 사용한다.
- 최종 캐릭터, 타일, 아이콘 스타일은 기능 완성 후 Figma 후보정 단계에서 통일한다.

## 9. 식물 NPC

- 식물은 학생 역할로 배정하지 않는다.
- 지정된 식물 생성 영역에 NPC 또는 채집 오브젝트로 나타난다.
- 초식동물이 먹으면 잠시 사라지고 일정 시간 후 다시 생성된다.
- 식물 섭취도 다른 먹이 관계와 동일하게 기록하고 점수를 준다.
- 화면에서 식물의 먹힘 여부와 재생 대기 상태가 분명하게 보여야 한다.

## 10. 먹이그물 결과 화면

먹이그물 화살표 방향은 과학 수업의 에너지 이동 방향에 맞춰 `먹이 → 먹는 생물`로 통일한다.

### 10.1 우리 반 개체 보기

- 학생 23명을 원형으로 배치한다.
- 각 노드에 닉네임, 생물 이름, 생물 그림을 표시한다.
- 실제 플레이 중 발생한 관계를 빨간색 화살표로 표시한다.
- 같은 관계가 여러 번 발생하면 선을 추가하지 않고 굵기 또는 횟수 배지로 표현한다.

### 10.2 생물 종류 보기

- 같은 생물 역할을 하나의 노드로 합친다.
- 학생들이 실제 플레이에서 나타나지 않은 관계를 파란색 선으로 추가한다.
- 두 노드를 선택해 방향선을 연결한다.
- 서버의 교과서 먹이 관계 데이터와 맞을 때만 확정한다.
- 정답이 아니면 답을 바로 공개하지 않고 다시 생각하도록 안내한다.

## 11. 생태계 비교 실험

실험 시작 직전에 서버가 전체 상태와 난수 시드를 저장한다.

### 11.1 실험 A

- 실제 플레이로 확인한 빨간 관계만 적용한다.
- 교사가 선택한 한 생물 종을 제거한다.
- 굶주림에 의한 사망을 활성화한다.
- 먹이를 충분히 먹으면 같은 종의 NPC 개체가 태어난다.
- 학생들이 직접 플레이한다.

### 11.2 실험 B

- 같은 시작 개체 수와 제거 생물을 사용한다.
- 빨간 관계와 학생들이 추가한 파란 관계를 모두 적용한다.
- 실험 A와 동일한 난수 시드를 사용한다.
- 공정한 비교를 위해 서버가 자동 시뮬레이션한다.
- 학생들은 유령 관찰자로 자유롭게 이동한다.

### 11.3 번식 제한

- 먹이 3회 섭취 시 NPC 한 개체 생성
- 번식 후 20초 대기
- 종별 최대 개체 수 제한
- 먹이가 부족하면 번식 중단
- 식물도 제한된 속도로 재생

실험 결과는 종별 개체 수 변화, 멸종한 종, 남은 먹이 관계를 A·B 화면에 나란히 표시한다. 게임의 승패가 아니라 먹이그물의 다양성이 생태계 변화에 어떤 차이를 만들었는지 토론하게 한다.

## 12. 실시간 네트워크 설계

- 서버 이동 로직은 Colyseus 고정 타임스텝으로 초당 30회 갱신한다.
- 클라이언트는 화면을 최대 초당 60회 렌더링한다.
- 내 이동은 Colyseus `Predict.reconciler`로 즉시 예측하고 서버 ACK 상태로 롤백·재생해 보정한다.
- 다른 학생은 `Predict`의 100ms 스냅샷 버퍼 사이를 보간해 부드럽게 표현한다.
- 위치, 상태, 점수, 배고픔, 라운드 단계는 동기화 상태로 관리한다.
- 먹기, 스킬, 알림, 이펙트는 일회성 메시지로 전달한다.

서버는 다음 항목을 검증한다.

- 최대 이동속도
- 벽 통과 여부
- 먹기 거리와 방향
- 먹이 관계
- 스킬 및 먹기 쿨타임
- 유령, 배탈, 방어 상태
- 중복 입력과 연속 요청

## 13. 구현 단계

### 현재 상태 (Figma 후보정 직전)

- 단계 1~6의 기능 MVP 구현 완료
- Colyseus 공식 예측·재조정과 원격 보간 적용 완료
- 전방 먹기 판정, 중립 타깃, 방향 표시, 액션 이펙트 적용 완료
- 교사용 밸런스 관찰 지표와 JSON 기록 적용 완료
- 데스크톱·태블릿 브라우저 흐름 및 23인 멀티플레이 자동 검증 통과
- 남은 현장 작업: 실제 태블릿 23대/학교 와이파이 장시간 리허설과 Figma 비주얼 후보정

### 단계 1. 프로젝트 기반

- npm workspace 구성
- React/Vite/Phaser 클라이언트 생성
- Colyseus 서버 생성
- 공통 타입 패키지 구성
- 개발·빌드·테스트 명령 구성
- 기본 게임 셸과 전체 화면 레이아웃 구현

완료 조건: 클라이언트와 서버를 한 명령으로 실행하고, 게임 화면에서 서버 연결 상태를 확인할 수 있다.

### 단계 2. 멀티플레이 수직 기능

- 교사 방 생성과 6자리 코드
- 학생 입장과 닉네임
- 최대 23명 접속
- 역할 배정과 역할 공개
- 타일맵과 캐릭터 이동
- 가상 조이스틱
- 다른 학생 위치 동기화
- 연결 끊김과 재접속

완료 조건: 23명이 같은 맵에서 각자의 캐릭터를 움직일 수 있다.

### 단계 3. 1·2판 핵심 규칙

- 서버 먹기 판정
- 식물 NPC
- 관계 발견 점수
- 잘못된 공격과 2초 배탈
- 굶주림과 30% 감속
- 먹힘, 유령, 같은 역할 리스폰
- 타이머와 맵 축소

완료 조건: 5분 라운드 전체를 진행하고 모든 먹기 기록을 서버에 남길 수 있다.

### 단계 4. 생물 스킬

- 공통 스킬 시스템
- 생물별 스킬 구현
- 쿨타임 HUD
- 스킬 서버 판정
- 역할 간 밸런스 테스트

완료 조건: 모든 역할이 하나의 스킬을 가지며 스킬 때문에 먹이 관계가 무시되지 않는다.

### 단계 5. 먹이그물

- 먹기 이벤트 집계
- 학생 23개체 그래프
- 생물 종류별 그래프
- 빨간 실제 기록선
- 파란 추가 관계선
- 교사 검토 및 확정
- 결과 이미지 저장

완료 조건: 한 라운드 기록으로 먹이그물을 만들고 학생들이 누락된 관계를 추가할 수 있다.

### 단계 6. 생태계 실험

- 특정 종 제거
- 굶주림 사망
- NPC 번식과 개체 수 제한
- 시작 상태 스냅샷
- 실험 A 직접 플레이
- 실험 B 자동 시뮬레이션
- 개체 수 변화 비교 화면

완료 조건: 같은 조건에서 먹이 관계가 적은 생태계와 풍부한 생태계의 변화를 비교할 수 있다.

### 단계 7. 수업 현장 QA

- 23개 봇 동시 접속 테스트
- 태블릿 터치 및 안전 영역 테스트
- 가로·세로 화면 대응
- 학교 와이파이 수준의 지연·패킷 손실 테스트
- 재접속과 교사 일시정지 테스트
- 한 수업 전체 흐름 리허설
- 결과 저장과 복원 확인

완료 조건: 실제 태블릿 23대 또는 동등한 부하에서 전체 수업 흐름을 중단 없이 진행할 수 있다.

### 단계 8. Figma 및 최종 디자인 후보정

- 실제 플레이 캡처를 기준으로 문제 화면 수집
- HUD 정보 우선순위 조정
- 역할 공개와 라운드 전환 연출 설계
- 버튼, 패널, 아이콘, 타이포그래피 디자인 시스템 확정
- 캐릭터와 맵 아트 스타일 통일
- 교사 화면과 학생 화면 구분 강화
- 확정된 Figma 디자인을 코드에 반영

이 단계에서는 기능 배치 자체를 전면 재설계하기보다, 검증된 조작 구조를 유지하면서 Roblox·ZEP 계열의 게임다운 시각 언어로 완성도를 높인다.

## 14. 테스트 및 품질 기준

- 23명이 10분 이상 접속해도 방 상태가 일치해야 한다.
- 먹기, 점수, 배탈, 방어, 리스폰 판정은 서버와 모든 클라이언트에서 동일해야 한다.
- 일반적인 태블릿에서 목표 60fps, 최소 30fps 이상을 유지해야 한다.
- 주요 게임 버튼의 터치 영역은 최소 48px 이상으로 한다.
- 게임 중 세로 스크롤과 브라우저 당겨서 새로고침을 방지한다.
- 연결이 잠시 끊겨도 30초 안에 같은 역할로 돌아올 수 있어야 한다.
- 교사는 어느 단계에서든 일시정지하고 안전하게 다음 단계로 이동할 수 있어야 한다.
- 학생 화면에는 개발자용 표, 폼, 대시보드처럼 보이는 UI를 노출하지 않는다.

## 15. 첫 번째 플레이 가능 버전의 범위

첫 개발 목표는 다음 수직 기능이다.

> 교사가 방을 만들고 학생 23명이 입장한 뒤, 각자 역할을 받아 2D 맵을 이동한다. 올바른 상대를 먹으면 관계가 기록되고, 먹힌 학생은 유령 상태 후 같은 역할로 리스폰한다. 5분 뒤 실제 기록이 빨간 먹이그물로 표시된다.

이 기능이 안정된 뒤 생물 스킬, 파란 선 추가, 생태계 비교 실험, Figma 후보정을 순차적으로 진행한다.

## 16. 예상 일정

1인 개발 기준의 초기 예상치다. 실제 에셋 제작과 학교 기기 테스트 여건에 따라 조정한다.

| 단계 | 예상 기간 |
| --- | ---: |
| 프로젝트 기반 | 2일 |
| 멀티플레이 수직 기능 | 4일 |
| 1·2판 핵심 규칙 | 5일 |
| 생물 스킬 | 3일 |
| 먹이그물 | 5일 |
| 생태계 실험 | 5일 |
| 수업 현장 QA | 4일 |
| Figma 및 최종 디자인 반영 | 별도 산정 |

기능 MVP는 약 3주, 생태계 비교 실험과 현장 검증까지 포함한 버전은 약 5~6주를 기준으로 한다.
