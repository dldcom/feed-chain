# 픽셀아트 UI 가이드

## 방향

밝은 16비트 숲 탐험 게임을 기준으로 한다. 학생 화면은 일반 웹 카드가 아니라 게임 HUD처럼 보여야 하며, 모든 상태에서 읽기 쉬운 큰 한글과 큰 터치 영역을 유지한다.

## 핵심 팔레트

| 용도 | 색상 |
| --- | --- |
| 가장 어두운 외곽선 | `#08251e` |
| HUD 패널 | `#0c382e` |
| 숲 중간색 | `#174b39` |
| 잔디 바닥 | `#639f47` |
| 밝은 잔디 픽셀 | `#8bc06a` |
| 크림색 | `#fff1b8` |
| 강조 노랑 | `#f6d365` |
| 먹기·경고 빨강 | `#ed5b4e` |
| 스킬 파랑 | `#3d86d8` |

## UI 규칙

- 테두리는 3~5px의 어두운 단색을 사용한다.
- 패널은 4~8px 모서리와 아래쪽 단색 블록 그림자를 사용한다.
- 학생 조작 버튼의 위치와 크기는 변경하지 않는다.
- 복잡한 배경에서도 한글은 밝은색과 어두운 외곽선으로 읽히게 한다.
- 픽셀 에셋은 `image-rendering: pixelated`와 Phaser `roundPixels`를 사용한다.
- 먹기 가능 여부를 색으로 정답처럼 알려주지 않는다.

## 생물 아틀라스

파일: `apps/client/public/assets/pixel/species-atlas.png`

```text
메뚜기 | 애벌레 | 토끼 | 다람쥐
개구리 | 직박구리 | 오리 | 뱀
족제비 | 매 | 풀 | 산수유
```

React에서는 `PixelSpeciesIcon`, Phaser에서는 `GameScene.setSpeciesSprite`가 같은 4×3 좌표를 사용한다.

## 생성 시안

- `artifacts/pixel-ui-direction.png`: 기존 태블릿 화면의 정보 구조를 유지한 픽셀아트 목표 시안
- `apps/client/public/assets/pixel/species-atlas.png`: 투명 배경의 12종 16비트 스프라이트 아틀라스

두 이미지는 Codex의 내장 `imagegen` 방식으로 만들었다. UI 시안 프롬프트는 기존 레이아웃을 보존하면서 16비트 숲 타일, 제한된 팔레트, 2~4px 외곽선, 단색 블록 그림자를 요구했다. 아틀라스 프롬프트는 4×3 고정 배열, 동일한 시각 크기, 투명 배경, 글자·격자·추가 오브젝트 금지를 요구했다.
